module.exports.counter = function (arr) {
    return arr.length + ' elements';
};

module.exports.sum = function (a, b) {
    return `The sum of the two numbers is ${a + b}`
}

module.exports.moduleName = 'Count module'; 

//module.exports += {
//    author: "Lee Cant"
//};